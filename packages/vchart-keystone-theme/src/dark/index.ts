import type { KeystoneTheme } from '../common';
import { theme } from '../common';

export const keystoneDesignDark: KeystoneTheme = {
  ...theme,
  name: 'keystoneDesignDark',
  description: 'Keystone Design - dark',
  type: 'dark'
};
