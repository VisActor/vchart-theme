import type { KeystoneTheme } from '../common';
import { theme } from '../common';

export const keystoneDesignLight: KeystoneTheme = {
  ...theme,
  name: 'keystoneDesignLight',
  description: 'Keystone Design - light',
  type: 'light'
};
